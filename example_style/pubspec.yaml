name: saropa_lints_example_style
description: Stylistic and testing fixture files for saropa_lints testing
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  saropa_lints_example:
    path: ../example
  crypto: ^3.0.0

dev_dependencies:
  saropa_lints:
    path: ../
