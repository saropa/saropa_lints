name: saropa_lints_self_check
description: Runs saropa_lints on the saropa_lints package itself to validate the codebase.
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dev_dependencies:
  saropa_lints:
    path: ../
