# Test fixtures for saropa_lints
# Only rules that are enabled in _enabledRules AND working are listed here
custom_lint:
  rules:
    # Security - WORKING
    - avoid_hardcoded_credentials

    # Collections - WORKING
    - avoid_unsafe_collection_methods
    - avoid_unsafe_reduce

    # NOTE: These rules are in _enabledRules but need investigation:
    # - avoid_logging_sensitive_data (not triggering as expected)
    # - avoid_eval_like_patterns (needs dart:mirrors)
    # - avoid_weak_cryptographic_algorithms (needs crypto package setup)
    # - avoid_late_final_reassignment (not triggering)
    # - avoid_unsafe_cast (not triggering)

    # NOTE: These rules are NOT in _enabledRules (disabled by default):
    # - avoid_null_assertion
    # - no_empty_block
    # - avoid_adjacent_strings
    # - avoid_continue
    # - prefer_contains
    # - prefer_first
    # - prefer_last
