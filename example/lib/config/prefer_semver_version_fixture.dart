// ignore_for_file: unused_element
// Fixture for prefer_semver_version.
// Rule checks pubspec.yaml version format (major.minor.patch); reports in lib/ files
// when version is not semver (e.g. 1.0 or v1.0.0). This file exists for fixture count;
// trigger depends on example pubspec.yaml version value.

void placeholderPreferSemverVersion() {}
