name: saropa_lints_example

description: Example and test fixtures for saropa_lints

publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  crypto: ^3.0.0
  meta: ^1.12.0

dev_dependencies:
  saropa_lints:
    path: ../
