# SAROPA LINTS — package development analysis options
#
# This file configures `dart analyze` for the saropa_lints package itself.
# Consumer projects use `dart run saropa_lints:init` to generate their own.
#
# Plugin is loaded from this package (same name); no self-dependency.
# Tiers: essential (fewest) | recommended | professional | comprehensive | pedantic (all ~2000, 20k+ issues).
# cspell:disable

include: package:saropa_lints/tiers/recommended.yaml

analyzer:
  errors:
    todo: ignore
    # Analyzer 9 marks ClassDeclaration.body as experimental; ignore for analyzer-9 compatibility (8.0.x).
    experimental_member_use: ignore
  exclude:
    # require_database_close false-positive (rule's runWithReporter contains openDatabase regex)
    - "lib/src/rules/resources/resource_management_rules.dart"
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.gr.dart"
    - "**/*.gen.dart"
    - "**/*.mocks.dart"
    - "**/*.config.dart"
    - "**/generated/**"
    - "build/**"
    - ".dart_tool/**"
    - "example/**"
    - "example_async/**"
    - "example_core/**"
    - "example_packages/**"
    - "example_platforms/**"
    - "example_style/**"
    - "example_widgets/**"
    - "self_check/**"
linter:
  rules:
    - curly_braces_in_flow_control_structures

plugins:
  saropa_lints:
    version: "8.1.0"
    # ═══════════════════════════════════════════════════════════════════
    # SAROPA LINTS CONFIGURATION
    # ═══════════════════════════════════════════════════════════════════
    # Regenerate with: dart run saropa_lints:init --tier essential
    # Tier: essential (291 of 2047 rules enabled)
    # Lint rules are disabled by default. Set to true to enable.
    # User customizations are preserved unless --reset is used
    #
    # Tiers (cumulative):
    #   1. essential    - Critical: crashes, security, memory leaks
    #   2. recommended  - Essential + accessibility, performance
    #   3. professional - Recommended + architecture, testing
    #   4. comprehensive - Professional + thorough coverage
    #   5. pedantic     - All rules (pedantic, highly opinionated)
    #   +  stylistic    - Opt-in only (formatting, ordering)
    #
    # Disabled platforms: macos, web, windows, linux
    #
    # Settings (max_issues, platforms, packages) are in analysis_options_custom.yaml
    # ═══════════════════════════════════════════════════════════════════

    diagnostics:
      #
      # ════════════════════════════════════════════════════════════════════════
      #   ✓ ENABLED RULES (ESSENTIAL TIER)
      # ════════════════════════════════════════════════════════════════════════
      #

      #
      # --- TIER 1: ESSENTIAL (291 rules) ---
      #
      always_remove_listener: true  # [WARNING] Listener added via addListener() but no matching removeListener() call found in dispose(). Orphaned listeners retain references to the widget, preventing garbage collection and causing memory leaks that accumulate as users navigate between screens. {v3}
      avoid_android_cleartext_traffic: true  # [WARNING] HTTP URL detected. Android 9+ blocks cleartext traffic by default. This request will fail silently. {v2}
      avoid_api_key_in_code: true  # [ERROR] Hardcoded keys are extractable from app binaries, enabling unauthorized API access and billing abuse. {v3}
      avoid_app_links_sensitive_params: true  # [ERROR] Deep link params are logged by OS and analytics, exposing tokens in crash reports and logs. {v2}
      avoid_assert_in_production: true  # [WARNING] assert() is compiled out of release builds by the Dart compiler. Any validation, input checking, or security enforcement using assert() will silently stop running in production, allowing invalid data, unauthorized access, or corrupted state to pass through unchecked. {v2}
      avoid_asset_manifest_json: true  # [ERROR] The 'AssetManifest.json' file was removed in Flutter 3.38.0 (PR #172594). Code that loads this path at runtime — such as rootBundle.loadString('AssetManifest.json') — will throw a FlutterError because the file no longer exists in the built app bundle. Migrate to the typed AssetManifest API (AssetManifest.loadFromAssetBundle) or use 'AssetManifest.bin' for the binary format. {v1}
      avoid_assigning_notifiers: true  # [ERROR] Reassigning Notifier breaks provider contract. State updates are lost and listeners receive stale data. {v3}
      avoid_async_callback_in_fake_async: true  # [ERROR] Async callback uses real time, making tests hang or timeout instead of using controlled fake time. {v2}
      avoid_async_in_build: true  # [ERROR] Build method should never be async. This will cause rendering issues. {v2}
      avoid_auth_in_query_params: true  # [ERROR] Query params are logged in server logs, browser history, and referrer headers, leaking auth tokens. {v3}
      avoid_bloc_context_dependency: true  # [WARNING] Bloc depending on BuildContext couples business logic to the UI layer. This makes the Bloc untestable in isolation, prevents reuse across widgets, and can cause crashes when the context becomes invalid after the widget is removed from the tree. {v6}
      avoid_bloc_event_mutation: true  # [ERROR] If BLoC events are mutable, they can be modified during processing, causing race conditions, unpredictable state changes, and hard-to-debug bugs. This breaks the contract of event immutability and can destabilize your app. {v3}
      avoid_blocking_main_thread: true  # [WARNING] Synchronous I/O blocks the main isolate, freezing the UI and causing jank or ANR (Application Not Responding) errors on mobile platforms. Even brief blocking degrades perceived performance and user experience significantly. {v1}
      avoid_builder_index_out_of_bounds: true  # [WARNING] itemBuilder accesses list without bounds check. If the index is out of bounds due to list changes, this will cause runtime exceptions, app crashes, and unpredictable UI behavior. This is a common source of production bugs in dynamic lists and can lead to negative user reviews. {v6}
      avoid_cached_image_unbounded_list: true  # [WARNING] CachedNetworkImage used inside a scrollable list without memCacheWidth or memCacheHeight. All images are cached at full resolution, causing excessive memory usage that can lead to OOM crashes on lower-end devices. In a long list, this quickly exhausts available memory. {v1}
      avoid_catch_all: true  # [WARNING] Bare catch clause without an on-type hides the specific error type being caught and silently swallows critical failures like OutOfMemoryError and StackOverflowError. This can mask fatal programming bugs, making them impossible to detect in crash reports or monitoring systems. {v3}
      avoid_catch_exception_alone: true  # [WARNING] Using "on Exception catch" without an "on Object catch" fallback silently misses all Error types including StateError, TypeError, and RangeError. These programming errors will crash the app without being logged or reported, making production debugging extremely difficult. {v2}
      avoid_circular_dependencies: true  # [WARNING] Service class constructor depends on another service of the same architectural layer (both end with Service, Repository, Controller, etc.). This creates circular dependency chains that break initialization order, prevent unit testing with mocks, and produce tightly coupled modules that cannot be modified or deployed independently. {v5}
      avoid_circular_provider_deps: true  # [ERROR] Circular dependencies between providers cause stack overflows, infinite loops, and unpredictable initialization order. This leads to runtime crashes, hard-to-debug errors, and broken state management. If not fixed, your app may crash or behave unpredictably in production. Always design provider graphs to be acyclic for reliability and maintainability. {v1}
      avoid_circular_redirects: true  # [ERROR] GoRoute redirect callback always returns a path and never returns null, creating an unconditional redirect that forms an infinite loop. The router exhausts the redirect limit and throws an exception, crashing the app or permanently locking users out of all navigation when the redirect chain has no termination condition. {v2}
      avoid_clearing_form_on_error: true  # [WARNING] Form fields are cleared when validation fails, destroying all user input. Users lose their partially-correct data and must re-enter everything from scratch. This frustrating pattern increases form abandonment, wastes user time, and creates a hostile experience that drives users away from completing the form submission. {v2}
      avoid_clipboard_sensitive: true  # [WARNING] Clipboard contents persist and are readable by other apps, exposing passwords and tokens. {v4}
      avoid_color_only_meaning: true  # [WARNING] Color is used as the sole visual indicator to convey meaning or state, which is inaccessible to approximately 8% of men and 0.5% of women with color vision deficiency. WCAG 1.4.1 (Use of Color) requires that color is never the only visual means of conveying information, indicating an action, or distinguishing a visual element. Always pair color with a secondary cue such as an icon, text label, underline, or pattern. {v1}
      avoid_conditional_hooks: true  # [ERROR] Hook function called conditionally. Hooks must be called unconditionally in the same order. {v2}
      avoid_connectivity_equals_internet: true  # [WARNING] ConnectivityResult compared as a proxy for internet availability. ConnectivityResult only indicates the transport layer (WiFi, mobile, ethernet) and does not verify actual internet reachability. A device can report WiFi while behind a captive portal, or mobile data with no route to the internet. Use an actual HTTP ping or DNS lookup to verify connectivity. {v1}
      avoid_context_across_async: true  # [ERROR] BuildContext used after await crashes if widget was disposed during the async gap. Check mounted first. {v6}
      avoid_context_after_await_in_static: true  # [ERROR] BuildContext used after await in static method. Context may be invalid after async gap. {v4}
      avoid_context_after_navigation: true  # [WARNING] BuildContext accessed after an awaited navigation call without a mounted check. The widget that owns this context is likely disposed by the time the await completes, and calling ScaffoldMessenger.of(), Navigator.of(), or Theme.of() on a disposed context throws a FlutterError that crashes the app or produces silent failures in release mode. {v3}
      avoid_context_dependency_in_callback: true  # [WARNING] BuildContext-dependent call (Theme.of, MediaQuery.of, Navigator.of, ScaffoldMessenger.of, etc.) is used inside an asynchronous callback such as Future.then(), Future.delayed(), or Timer. After an async gap the originating widget may have been unmounted, making the captured BuildContext stale and causing a "Looking up a deactivated widget's ancestor" exception at runtime. {v2}
      avoid_datetime_now_in_tests: true  # [WARNING] DateTime.now() in tests produces non-deterministic values that vary between runs, causing flaky assertions that pass locally but fail in CI, making test failures impossible to reproduce reliably. {v2}
      avoid_deep_link_sensitive_params: true  # [ERROR] Deep link query parameter contains sensitive data (password, token, secret, API key, or credential), creating a security breach. Deep link URLs are recorded in system logs, browser history, HTTP referrer headers, and analytics platforms, permanently exposing credentials to attackers who gain access to those logs or the device history. {v4}
      avoid_deprecated_crypto_algorithms: true  # [WARNING] The use of outdated cryptographic algorithms such as MD5, SHA1, DES, 3DES, and RC4 exposes your application to well-known attacks. These algorithms have been broken by the security community: MD5 and SHA1 are vulnerable to collision and preimage attacks, allowing attackers to forge digital signatures or tamper with data. DES and 3DES have insufficient key lengths and are susceptible to brute-force attacks, while RC4 is vulnerable to several biases and key recovery attacks. Continuing to use these algorithms puts all encrypted or hashed data at risk of compromise, regardless of other security measures. {v2}
      avoid_dialog_context_after_async: true  # [ERROR] If you call Navigator.pop() or use BuildContext after an await, the widget may have been disposed, causing a "Looking up deactivated widget" crash. This leads to app instability and poor user experience. {v5}
      avoid_dialog_in_build: true  # [ERROR] Calling showDialog (or similar) inside build() will cause your app to enter an infinite loop, repeatedly showing dialogs and freezing the UI. This results in a poor user experience and may crash the app. {v2}
      avoid_double_for_money: true  # [ERROR] Floating point causes rounding errors in money calculations. $0.1 + $0.2 != $0.3, causing financial loss. {v4}
      avoid_drift_enum_index_reorder: true  # [ERROR] Drift TypeConverter uses enum ordinal index for int storage. If enum values are reordered or new values are inserted before existing ones, all persisted data silently maps to wrong enum values, causing data corruption without any error. {v1}
      avoid_drift_raw_sql_interpolation: true  # [ERROR] String interpolation or concatenation detected in Drift raw SQL method. This creates a SQL injection vulnerability (OWASP A03:2021-Injection). Attackers can execute arbitrary SQL including data exfiltration and deletion. {v1}
      avoid_duplicate_bloc_event_handlers: true  # [ERROR] Second handler for same event type is ignored, causing silent bugs when expected logic runs. {v2}
      avoid_duplicate_map_keys: true  # [WARNING] Duplicate key in map literal silently overwrites the earlier value, causing data loss and unpredictable behavior. Only the last value assigned to the key will persist in the resulting map. {v4}
      avoid_duplicate_widget_keys: true  # [WARNING] Multiple widgets in a list share the same Key value. Flutter uses keys to match old widgets with new widgets during reconciliation. Duplicate keys cause the framework to reuse the wrong element, leading to stale state, broken animations, and incorrect widget ordering after list mutations. {v4}
      avoid_dynamic_code_loading: true  # [ERROR] Dynamic code loading at runtime bypasses compile-time dependency verification, allowing execution of unverified code and creating supply chain attack vectors. {v3}
      avoid_dynamic_sql: true  # [ERROR] Constructing SQL queries by concatenating or interpolating user input directly into the query string exposes your application to SQL injection attacks. Attackers can craft input that alters the structure of your query, allowing them to read, modify, or delete arbitrary data, escalate privileges, or compromise the entire database. SQL injection is one of the most critical and well-documented security vulnerabilities in software development, and has led to major data breaches across industries. {v4}
      avoid_eval_like_patterns: true  # [ERROR] Dynamic code execution allows arbitrary code injection, enabling attackers to execute malicious code. {v7}
      avoid_excessive_rebuilds_animation: true  # [WARNING] The builder callback of this animation widget contains too many widget constructors, causing the entire subtree to rebuild on every animation frame (typically 60 times per second). This wastes CPU cycles, increases battery drain, and degrades animation smoothness. Only widgets that actually change during the animation should be inside the builder; static content should be passed via the child parameter or moved outside. {v1}
      avoid_existing_instances_in_bloc_provider: true  # [ERROR] Returning an existing bloc instance from BlocProvider(create: ...) causes the bloc to be closed when the provider disposes, even if it is still used elsewhere. This can lead to unexpected state loss, runtime errors, and hard-to-debug bugs. Always use the correct provider pattern for new vs. existing blocs. {v2}
      avoid_expanded_outside_flex: true  # [ERROR] Expanded, Flexible, and Spacer set FlexParentData on their child, which only RenderFlex (Row, Column, Flex) can read during layout. Placing them inside any other parent — Stack, Center, Padding, LimitedBox, SizedBox, etc. — throws an unrecoverable "Incorrect use of ParentDataWidget" FlutterError at runtime. This also happens indirectly when a widget's build() returns Expanded and the widget is later wrapped by a non-Flex container, breaking the Flex→Expanded parent chain. {v7}
      avoid_external_storage_sensitive: true  # [ERROR] External storage is world-readable on Android, exposing credentials to any installed app. {v4}
      avoid_fixed_size_in_scaffold_body: true  # [WARNING] Scaffold body has a Column with text input fields but no ScrollView. The keyboard will cause overflow. {v2}
      avoid_flashing_content: true  # [WARNING] Flashing >3Hz can trigger seizures in users with photosensitive epilepsy. WCAG 2.3.1 compliance required. {v4}
      avoid_functions_in_register_singleton: true  # [WARNING] registerSingleton expects an instance, not a factory function. {v3}
      avoid_future_in_build: true  # [WARNING] Creating Future in build() causes repeated async calls on every rebuild. {v2}
      avoid_getx_build_context_bypass: true  # [ERROR] Get.context or Get.overlayContext bypasses Flutter's BuildContext propagation. This hides widget tree dependencies, makes code untestable without GetX runtime, and circumvents Flutter's fundamental context-based service location pattern. The retrieved context may be stale or from an unexpected part of the widget tree, causing subtle bugs. {v2}
      avoid_getx_context_outside_widget: true  # [WARNING] Get.context or Get.overlayContext used outside widget class. This is unsafe and may cause crashes. {v2}
      avoid_global_key_in_build: true  # [ERROR] Creating a GlobalKey inside the build() method causes it to be recreated on every rebuild, which results in lost widget state, broken references, and unpredictable UI behavior. This can cause your app to lose user input or fail to maintain state across rebuilds. {v4}
      avoid_go_router_inline_creation: true  # [WARNING] GoRouter instance created inside build() is destroyed and recreated on every widget rebuild. This resets all navigation state including the current route stack, destroys in-flight transitions, breaks hot reload during development, and causes users to lose their navigation position whenever the parent widget rebuilds. {v3}
      avoid_hardcoded_config: true  # [WARNING] Hardcoded configuration value detected. Embedding URLs, ports, API keys, or feature flags directly in source code makes the app inflexible across environments (dev, staging, production) and forces a rebuild for every configuration change, increasing deployment risk. {v4}
      avoid_hardcoded_credentials: true  # [ERROR] Hardcoding credentials (such as passwords, API keys, or tokens) in source code exposes them to anyone with access to the codebase, including public repositories and version control history. This can lead to unauthorized access, data breaches, and compromised systems. Credentials should always be stored securely and never committed to version control. {v5}
      avoid_hardcoded_encryption_keys: true  # [ERROR] Hardcoded encryption keys present in source code or binaries can be easily extracted by attackers using reverse engineering tools. Once exposed, these keys allow adversaries to decrypt all user data protected by the key, resulting in a complete compromise of confidentiality for every user of your application. This vulnerability is especially critical in mobile and web apps, where binaries are distributed to end users. Hardcoded keys are often found in test code, examples, or as quick fixes, but they must never be used in production or shipped code. Attackers routinely scan for such patterns, and automated tools can detect and extract these secrets within minutes of release. {v5}
      avoid_hidden_interactive: true  # [ERROR] This interactive element uses excludeFromSemantics, making it completely inaccessible to screen readers and users with assistive technologies. Excluding interactive child elements from the semantics tree prevents users with disabilities from discovering or activating key actions, which fails accessibility standards and can break critical workflows. {v3}
      avoid_hive_field_index_reuse: true  # [ERROR] Reusing Hive field indexes across different fields or types can corrupt your database, cause data loss, and make migrations impossible. This can result in users losing critical data or experiencing app crashes after updates. Always assign unique field indexes and never change them after release. See https://docs.hivedb.dev/#/adapters/fields. {v3}
      avoid_hive_synchronous_in_ui: true  # [WARNING] Synchronous Hive box operation (get, put, delete, add) is called inside a build() or initState() method. These operations perform disk I/O on the main isolate, blocking frame rendering and causing visible UI jank. On slower devices or with large boxes, this can trigger ANR (Application Not Responding) dialogs, force-close the app, and create a poor user experience that leads to negative app store reviews. {v2}
      avoid_hooks_outside_build: true  # [ERROR] Hook function called outside of build method. Hooks must only be called from build(). {v2}
      avoid_ignoring_ssl_errors: true  # [ERROR] Bypassing or ignoring SSL/TLS certificate errors disables the core security guarantees of HTTPS, allowing man-in-the-middle attackers to intercept, modify, or steal all data sent over the network. This exposes user credentials, tokens, and sensitive personal data to silent compromise, and is a leading cause of real-world security breaches. SSL validation must never be disabled in production code. {v3}
      avoid_inherited_widget_in_initstate: true  # [WARNING] InheritedWidget accessed in initState(), where the widget is not yet fully mounted in the element tree. This call returns stale or missing data and does not subscribe to updates, so the widget never rebuilds when the inherited value changes. {v4}
      avoid_instantiating_in_bloc_value_provider: true  # [ERROR] Creating a new bloc instance inside BlocProvider.value prevents the bloc from being automatically closed, leading to memory leaks and unpredictable state. This is a critical resource management issue that can degrade app performance and reliability. {v2}
      avoid_instantiating_in_value_provider: true  # [ERROR] Creating a new instance inside Provider.value prevents proper lifecycle management, leading to memory leaks, resource retention, and unpredictable behavior. This is a critical issue for stateful objects like ChangeNotifiers and ValueListenables. {v2}
      avoid_ios_in_app_browser_for_auth: true  # [ERROR] OAuth URL detected in WebView. Google and Apple block OAuth via in-app WebView for security reasons. {v2}
      avoid_isar_enum_field: true  # [ERROR] Storing enums directly in Isar collections is dangerous: renaming or reordering enum values will silently corrupt your data, breaking existing records and causing unpredictable bugs. {v3}
      avoid_keyboard_overlap: true  # [INFO] TextField may be hidden behind the soft keyboard when no viewInsets handling is detected. Users cannot see what they are typing, leading to input errors, frustration, and accessibility failures on devices where the keyboard covers a large portion of the screen. {v5}
      avoid_late_final_reassignment: true  # [WARNING] Late final field has multiple assignment paths, which throws a LateInitializationError at runtime on the second write. The compiler cannot catch this statically, so the crash only surfaces during execution of the specific code path that triggers the duplicate assignment. {v4}
      avoid_listen_in_async: true  # [WARNING] Using context.watch() inside an async callback triggers widget rebuilds during asynchronous execution, creating stale closures that capture outdated state. This causes data races where async operations complete with wrong values, leading to corrupted state or duplicate side effects. {v4}
      avoid_loading_full_pdf_in_memory: true  # [WARNING] Loading an entire PDF file into memory can cause out-of-memory errors, especially on mobile devices with limited RAM. This can crash the app, degrade performance, and prevent users from viewing large documents. {v3}
      avoid_logging_sensitive_data: true  # [ERROR] Logging sensitive data such as passwords, tokens, or personally identifiable information (PII) exposes users to privacy risks and can result in data leaks if logs are accessed by unauthorized parties. Sensitive information in logs may appear in crash reports, debug output, or be inadvertently shared with third parties, leading to compliance violations and loss of user trust. Always sanitize logs to prevent exposure of confidential data. {v6}
      avoid_long_running_isolates: true  # [WARNING] Long-running isolate detected. iOS kills background tasks after ~30 seconds. Design tasks to complete quickly. {v3}
      avoid_losing_stack_trace: true  # [WARNING] Rethrowing without preserving the stack trace loses the original error location and call chain. Production debugging becomes impossible because crash reports show only the rethrow site, not where the error actually originated. Always capture and forward the full stack trace. {v2}
      avoid_misused_hooks: true  # [WARNING] Hook function is called inside a callback or closure, which violates the rules of hooks. Hooks must be called at the top level of the build method, never inside callbacks (onPressed, onTap), event handlers, or nested functions. Hooks called inside closures execute unpredictably because the closure may run zero, one, or many times per build, breaking hook state tracking and causing runtime errors or lost state. {v1}
      avoid_mixed_environments: true  # [ERROR] Mixing production and development configuration in your codebase creates serious security vulnerabilities. Debug APIs may expose sensitive data, development endpoints can corrupt production databases, and credentials may leak to unauthorized users. This can result in data breaches, compliance violations, and loss of user trust. Environment-specific configuration must be strictly separated and managed. {v4}
      avoid_mounted_in_setstate: true  # [WARNING] Checking the mounted property inside a setState callback is an anti-pattern. If the widget is not mounted, setState should not be called at all. Placing the check inside the callback can lead to subtle bugs where partial state updates execute before the mounted check runs. {v5}
      avoid_multiple_material_apps: true  # [ERROR] Multiple MaterialApp (or CupertinoApp) widgets in the tree create separate Navigator stacks, separate Theme contexts, and independent Locale/MediaQuery scopes. This breaks navigation (pushNamed cannot reach routes in the other app), causes theme inconsistencies, and doubles memory usage for shared resources. {v2}
      avoid_mutable_field_in_equatable: true  # [ERROR] Equatable classes should only have final fields. Mutable fields break value equality, causing bugs in collections, state management, and UI updates. Changing a field after object creation makes == and hashCode unreliable. {v4}
      avoid_nested_scaffolds: true  # [WARNING] Nested Scaffold widget detected inside another Scaffold. This creates duplicate app bars, floating action buttons, and bottom navigation, leading to broken layout, gesture conflicts, and a confusing user experience. {v5}
      avoid_notification_same_id: true  # [WARNING] Static or hardcoded notification ID causes newer notifications to silently replace older ones using the same identifier. Users will miss important alerts, messages, and time-sensitive updates without any indication that previous notifications were overwritten, leading to lost information and degraded communication reliability. {v3}
      avoid_notification_silent_failure: true  # [WARNING] Notification operation without error handling. Failures will be silent and hard to debug. {v2}
      avoid_null_assertion: true  # [INFO] Null assertion operator (!) throws a runtime exception if the value is null, crashing the app without a meaningful error message. The resulting _CastError provides no context about which value was null or why, making production crashes difficult to diagnose from error reports alone. {v7}
      avoid_openai_key_in_code: true  # [ERROR] Hardcoded OpenAI keys are extractable from binaries, enabling API abuse charged to your account. {v2}
      avoid_optional_field_crash: true  # [ERROR] JSON field accessed with direct bracket notation on a Map that may contain null values. When the API response omits an optional field, this direct access throws a runtime exception that crashes the app. Defensive null-aware access with ?[] prevents unexpected null pointer errors and provides graceful handling of incomplete or malformed JSON responses. {v2}
      avoid_overlapping_animations: true  # [WARNING] Multiple animations targeting the same property (e.g., opacity, position, scale) at the same time cause visual jitter, unpredictable results, and confusing UI behavior. Overlapping animations can make transitions hard to follow and degrade user experience, especially for users with cognitive or visual disabilities. {v4}
      avoid_parameter_mutation: true  # [WARNING] Parameter object is being mutated. This modifies the caller's data. {v2}
      avoid_path_traversal: true  # [WARNING] File paths constructed from user input may allow path traversal attacks (e.g., "../"), enabling access to sensitive files outside the intended directory. This is a critical security risk. {v8}
      avoid_permission_handler_null_safety: true  # [ERROR] Deprecated pre-null-safety permission_handler API detected. The PermissionHandler() constructor and PermissionGroup enum were removed in permission_handler 8.0+. Using these deprecated APIs prevents migration to null-safe versions and causes compile errors when updating the package. The modern API uses Permission.camera.status and Permission.camera.request() instead. {v2}
      avoid_positioned_outside_stack: true  # [ERROR] Positioned widget used outside of a Stack. This causes a ParentData crash at runtime. {v5}
      avoid_print_error: true  # [WARNING] Using print() for error logging in a catch block. In production, print() output is not captured by crash reporting services like Crashlytics or Sentry, making errors invisible to monitoring dashboards. Errors logged only via print() are effectively lost and cannot trigger alerts or be tracked over time. {v2}
      avoid_print_in_release: true  # [ERROR] Using print() in production exposes debug information to end users, can leak sensitive data, and negatively impacts performance. Print statements are not optimized for release builds and may clutter logs, making it harder to diagnose real issues. This can also violate privacy policies and app store guidelines. {v3}
      avoid_provider_value_rebuild: true  # [WARNING] Provider.value with inline object creation recreates the notifier on every widget build. This discards accumulated user data, resets form state, and can trigger infinite rebuild loops that freeze the UI as each rebuild creates a new instance that triggers another rebuild. {v6}
      avoid_purchase_in_sandbox_production: true  # [ERROR] Hardcoded IAP environment URL detected. Sandbox receipts fail in production and vice versa. {v3}
      avoid_recursive_widget_calls: true  # [ERROR] Widget build method creates a new instance of itself, triggering infinite recursion. This crashes the app with a stack overflow as Flutter repeatedly builds the same widget, consuming all available stack frames within milliseconds. {v6}
      avoid_redirect_injection: true  # [WARNING] Redirect URL read from a parameter or user input without domain validation creates an open redirect vulnerability. Attackers craft URLs pointing to your app that redirect victims to phishing sites, credential harvesters, or malware downloads while appearing to originate from your trusted domain. {v5}
      avoid_ref_in_build_body: true  # [ERROR] Using ref.read() in build() does not trigger widget rebuilds when the provider changes, leading to stale UI, missed updates, and confusing bugs. This breaks the reactive model of Riverpod and can cause your app to display outdated information. {v5}
      avoid_ref_in_dispose: true  # [ERROR] Using ref in dispose() is unsafe because the provider may already be destroyed, leading to runtime errors, crashes, or accessing invalid state. This can cause unpredictable bugs and app instability. {v1}
      avoid_ref_inside_state_dispose: true  # [ERROR] Ref may already be disposed when dispose() runs, causing "already disposed" errors or crashes. {v3}
      avoid_ref_read_inside_build: true  # [WARNING] ref.read() called inside build() bypasses Riverpod reactivity. The widget will not rebuild when the provider state changes, resulting in stale data displayed to the user. This creates inconsistent UI state that fails silently and produces hard-to-diagnose rendering errors across dependent widgets. {v3}
      avoid_ref_watch_outside_build: true  # [ERROR] ref.watch() detected outside build() method, breaking the Riverpod widget lifecycle. Subscriptions created outside build() leak memory, produce stale data, and cause missed UI updates that lead to inconsistent state and hard-to-debug rendering errors across dependent widgets. {v4}
      avoid_retaining_disposed_widgets: true  # [ERROR] Non-widget class stores a reference to a Widget or State object. After the widget is disposed, this reference prevents garbage collection and may cause use-after-dispose crashes. Widget and State objects are tied to the Flutter lifecycle and should not be retained outside the widget tree. This creates a strong reference that outlives the widget's lifecycle. {v2}
      avoid_riverpod_state_mutation: true  # [WARNING] Riverpod state mutated directly instead of replaced. Direct mutations don't trigger Notifier rebuilds, causing the UI to display stale, inconsistent data. Listeners and consumers will not receive the updated values. {v2}
      avoid_scroll_listener_in_build: true  # [WARNING] Scroll listener registered in build() accumulates duplicate subscriptions on every widget rebuild. Each rebuild adds another listener that is never removed, causing memory leaks, duplicate callback executions, and progressively degrading scroll performance as listeners compound over the widget lifecycle. {v2}
      avoid_scrollable_in_intrinsic: true  # [ERROR] Scrollable widget inside IntrinsicHeight/IntrinsicWidth causes a geometry loop crash. Scrollables have no natural size. {v2}
      avoid_sensitive_data_in_clipboard: true  # [WARNING] Copying sensitive data such as passwords, tokens, or personal information to the clipboard exposes users to data theft, as other apps or malicious actors can access clipboard contents. This is a significant privacy and security risk, especially on shared or compromised devices. See https://owasp.org/www-project-top-ten/2017/A3_2017-Sensitive_Data_Exposure.html and https://api.flutter.dev/flutter/services/Clipboard-class.html. {v3}
      avoid_sensitive_in_logs: true  # [ERROR] Logging sensitive data (such as passwords, tokens, or personal information) exposes users to credential theft, privacy violations, and compliance failures (e.g., OWASP A09). Attackers or support staff may access logs and extract secrets, leading to data breaches. {v5}
      avoid_setstate_in_build: true  # [ERROR] Calling setState inside the build() method causes the widget to rebuild recursively, leading to stack overflows, app crashes, and unpredictable UI behavior. This makes your app unstable and difficult to debug. Always trigger state changes outside build(). {v6}
      avoid_shared_prefs_in_isolate: true  # [ERROR] SharedPreferences used in isolate context. SharedPreferences does not work in isolates. {v2}
      avoid_shared_prefs_sensitive_data: true  # [ERROR] SharedPreferences stores data as plaintext XML, readable via backup extraction or rooted device. {v3}
      avoid_shrink_wrap_expensive: true  # [WARNING] shrinkWrap: true disables list virtualization, forcing Flutter to measure and lay out every item upfront instead of lazily building only visible items. For lists with many children, this causes slow initial rendering, high memory consumption, and jank during scrolling as the framework processes the entire list eagerly. {v5}
      avoid_spacer_in_wrap: true  # [ERROR] Spacer/Expanded inside Wrap causes a flex paradox crash. Wrap does not support flex-based sizing. {v3}
      avoid_sqflite_type_mismatch: true  # [ERROR] SQLite type may not match Dart type. Booleans are stored as INTEGER (0/1), DateTime as TEXT/INTEGER. {v2}
      avoid_storing_context: true  # [ERROR] Storing a BuildContext in a field or variable for later use is dangerous because the context may become invalid after the widget is disposed or rebuilt. Using a stale context can cause exceptions, show dialogs on the wrong screen, or trigger subtle bugs. This is a common source of crashes and hard-to-diagnose UI issues in Flutter apps. {v4}
      avoid_storing_passwords: true  # [ERROR] SharedPreferences stores passwords in plaintext, readable by anyone with device access or backup. {v4}
      avoid_storing_sensitive_unencrypted: true  # [ERROR] Unencrypted sensitive data exposed via device backup extraction or rooted device access, enabling identity theft. {v4}
      avoid_stream_in_build: true  # [ERROR] Creating a Stream or StreamController inside a widget's build() method causes a new stream instance to be created on every rebuild of that widget. This leads to multiple overlapping subscriptions, memory leaks, and lost or duplicated events, making the widget's state unpredictable and difficult to debug. Always manage streams as persistent fields in the State class, not as local variables in build(). See https://docs.flutter.dev/cookbook/networking/web-sockets#using-streambuilder. {v2}
      avoid_supabase_anon_key_in_code: true  # [ERROR] Hardcoded keys can be extracted from app binary. Attackers gain direct access to your Supabase project. {v2}
      avoid_swallowing_exceptions: true  # [WARNING] Catch block swallows exception without logging, rethrowing, or handling it. Silent failures hide production bugs, break monitoring and alerting systems, and make it impossible to diagnose issues reported by users. Every caught exception must be acknowledged. {v5}
      avoid_table_cell_outside_table: true  # [ERROR] TableCell used outside of a Table widget. This causes a ParentData crash at runtime. {v4}
      avoid_test_sleep: true  # [WARNING] Real sleep() or Future.delayed() call in test code blocks the test runner, slows the suite, and produces flaky failures when system load varies. Tests with real delays waste CI minutes and give inconsistent results across machines. {v5}
      avoid_textfield_in_row: true  # [ERROR] TextField/TextFormField inside a Row without width constraints causes an unbounded width crash. {v3}
      avoid_throw_in_finally: true  # [WARNING] Throw statement inside a finally block silently replaces the original exception. If the try block throws an error, the finally throw overwrites it, making the root cause invisible in crash logs and error handlers. {v5}
      avoid_token_in_url: true  # [ERROR] Tokens in URLs are logged in browser history, server logs, and referrer headers, exposing credentials. {v5}
      avoid_unassigned_stream_subscriptions: true  # [WARNING] Stream subscription created by listen() is not assigned to a variable. Without a reference to the StreamSubscription, you cannot cancel it during dispose, which causes memory leaks, prevents garbage collection, and allows callbacks to fire after the StatefulWidget has been destroyed and unmounted from the widget tree. {v5}
      avoid_unawaited_future: true  # [WARNING] Not awaiting a Future means any errors or exceptions it throws may be silently lost, leading to missed bugs and unreliable app behavior. This is especially risky in production code where error reporting is critical. {v3}
      avoid_unbounded_cache_growth: true  # [WARNING] Map or collection used as a cache has no maximum size constraint. Without eviction logic, the cache grows indefinitely as new entries are added, consuming increasing amounts of device memory. This eventually exhausts available RAM and crashes the app with an out-of-memory error, particularly on mobile devices with limited memory resources. {v4}
      avoid_unbounded_listview_in_column: true  # [ERROR] Scrollable widget inside a Column without Expanded/Flexible causes an unbounded constraints crash. {v3}
      avoid_uncaught_future_errors: true  # [WARNING] Fire-and-forget Future called without error handling. Any exception thrown by this Future is silently lost or crashes the app via the global error handler. Without .catchError(), try-catch, or .ignore(), async failures become invisible in production and extremely difficult to diagnose from crash logs. {v5}
      avoid_unconstrained_dialog_column: true  # [WARNING] Column inside a dialog without mainAxisSize: MainAxisSize.min can overflow and push buttons off-screen. {v2}
      avoid_undisposed_instances: true  # [WARNING] Disposable object (e.g., TextEditingController, AnimationController, StreamController) created but no matching dispose() call found. Undisposed instances retain listeners, streams, and platform resources, causing memory leaks that grow with each widget rebuild or navigation. {v7}
      avoid_unreachable_for_loop: true  # [WARNING] For loop with impossible bounds will never execute, resulting in dead code. This often indicates a logic error, typo, or incorrect increment direction. Unreachable loops can hide bugs, confuse maintainers, and lead to missed updates or calculations. {v3}
      avoid_unsafe_collection_methods: true  # [WARNING] Calling .first, .last, or .single on an empty collection throws a StateError at runtime, crashing the app. This is especially dangerous when the collection comes from an API response, database query, or user input where emptiness cannot be guaranteed at compile time. {v10}
      avoid_unsafe_deserialization: true  # [WARNING] Unvalidated JSON from untrusted sources can crash the app, inject malicious data, or corrupt application state. Attackers can exploit this vulnerability to trigger unexpected behavior, bypass security checks, or cause data loss by crafting malicious payloads. {v4}
      avoid_unsafe_reduce: true  # [WARNING] Calling reduce() on an empty collection throws a StateError at runtime, crashing the app. Unlike fold(), reduce() has no initial value and requires at least one element to operate. {v7}
      avoid_unsafe_where_methods: true  # [WARNING] Calling firstWhere, lastWhere, or singleWhere without an orElse callback throws a StateError when no element matches the predicate. This crashes the app at runtime, especially when filtering data from external sources where matches are not guaranteed. {v4}
      avoid_unverified_native_library: true  # [ERROR] Loading native libraries from dynamic or absolute paths bypasses package verification, allowing attackers to substitute malicious native code. {v3}
      avoid_user_controlled_urls: true  # [WARNING] Allowing user-controlled URLs to be loaded without validation exposes your app to security vulnerabilities such as phishing, open redirects, and malicious content injection. This can compromise user data and app integrity. Always validate and sanitize URLs before loading or displaying them. See https://owasp.org/www-community/attacks/Unvalidated_Redirects_and_Forwards_Cheat_Sheet and https://pub.dev/packages/webview_flutter#security-considerations. {v3}
      avoid_weak_cryptographic_algorithms: true  # [WARNING] Weak or deprecated cryptographic algorithm detected (e.g. MD5, SHA-1). These algorithms have known collision vulnerabilities that allow attackers to forge hashes, compromising data integrity verification, password storage, and digital signature validation. {v5}
      avoid_yield_in_on_event: true  # [ERROR] yield breaks Bloc 8.0+ concurrency and event ordering, causing unpredictable state updates. {v3}
      check_is_not_closed_after_async_gap: true  # [ERROR] Emitting to closed Bloc throws StateError, crashing the app when widget is disposed during async. {v2}
      check_mounted_after_async: true  # [ERROR] setState() after await without mounted check. State may be disposed during async gap, causing "setState() called after dispose()" crash. {v5}
      dispose_class_fields: true  # [WARNING] Class has disposable fields but no dispose/close method. This may cause memory leaks. {v2}
      dispose_provided_instances: true  # [WARNING] Provider creates a disposable instance without a dispose callback, causing memory leaks. Undisposed stream subscriptions and controllers persist after the parent StatefulWidget is removed from the tree, continuing to hold resources and fire callbacks that trigger setState-after-dispose errors. {v4}
      emit_new_bloc_state_instances: true  # [ERROR] Mutating state object breaks equality checks, preventing BlocBuilder from detecting changes. {v2}
      incorrect_firebase_event_name: true  # [ERROR] Invalid event name is silently dropped by Firebase Analytics. Your analytics data will be incomplete. {v2}
      incorrect_firebase_parameter_name: true  # [ERROR] Invalid parameter names are silently dropped by Firebase. Event data will be missing fields. {v2}
      list_all_equatable_fields: true  # [WARNING] Equatable class has fields not included in props. Equality checks fail silently when these fields differ, causing inconsistent behavior in collections, comparisons, and UI updates. Two objects with different field values will be treated as equal, leading to missed rebuilds, incorrect deduplication, and subtle bugs that are extremely hard to trace. {v3}
      missing_test_assertion: true  # [WARNING] Test without assertions always passes, giving false confidence. Bugs slip through undetected. {v5}
      pass_existing_future_to_future_builder: true  # [WARNING] Creating new Future in FutureBuilder restarts the async operation on every widget rebuild. This causes duplicate network calls, database queries, and slow UI with visible loading states. {v8}
      pass_existing_stream_to_stream_builder: true  # [WARNING] New Stream created inline in the StreamBuilder constructor. Every build() call creates a fresh stream, discarding the previous subscription and triggering an infinite rebuild loop as each new stream emits its initial value. {v7}
      prefer_copy_with_for_state: true  # [ERROR] Directly modifying BLoC state breaks immutability, leading to unpredictable UI updates, missed rebuilds, and subtle bugs that surface only in production. The BLoC pattern relies on immutable state transitions to guarantee that every emit triggers a rebuild; mutating fields in place silently bypasses this contract. {v4}
      prefer_expanded_at_call_site: true  # [ERROR] Expanded/Flexible/Spacer returned from build() forces flex layout on all callers, breaking reuse in non-flex contexts. If this widget is placed inside a Stack, SingleChildScrollView, or any non-flex parent, the Expanded wrapper triggers a runtime ParentDataWidget error and crashes the app. {v4}
      prefer_firebase_remote_config_defaults: true  # [INFO] Missing defaults cause null/zero values when fetch fails, breaking app behavior. {v3}
      prefer_html_escape: true  # [WARNING] Displaying user-generated content in a WebView without proper HTML escaping exposes your app to cross-site scripting (XSS) attacks. Malicious input can execute scripts, steal user data, or compromise device security. Always sanitize and escape user content before rendering it in a web context. {v4}
      prefer_image_picker_max_dimensions: true  # [WARNING] pickImage() called without maxWidth or maxHeight parameters loads full-resolution camera images (12+ megapixels on modern devices). Decoding these large images into memory causes OutOfMemoryError crashes on lower-end devices, excessive memory consumption that triggers OS app kills, and slow image processing. {v2}
      prefer_lazy_loading_images: true  # [WARNING] Image.network in ListView builder without lazy loading. All images load immediately, wasting bandwidth and memory. {v2}
      prefer_pending_intent_flags: true  # [ERROR] PendingIntent without FLAG_IMMUTABLE or FLAG_MUTABLE crashes on Android 12+. {v2}
      prefer_platform_io_conditional: true  # [ERROR] Platform class from dart:io throws UnsupportedError at runtime on Flutter web because dart:io is unavailable in browser environments. Accessing Platform.isAndroid, Platform.isIOS, or any Platform property without first checking kIsWeb crashes the web app immediately with an unrecoverable runtime error. {v2}
      prefer_secure_random_for_crypto: true  # [WARNING] The default Random() constructor in Dart uses a seed based on the current system time, making its output predictable and vulnerable to attack. If Random() is used to generate cryptographic keys, initialization vectors (IVs), nonces, tokens, or any value intended to protect sensitive data, attackers can reproduce the same sequence of random numbers and break your security. This flaw has led to real-world breaches where cryptographic protections were bypassed due to weak randomness. Only a cryptographically secure random number generator (CSPRNG) can provide the unpredictability required for security-critical operations. {v3}
      prefer_timeout_on_requests: true  # [WARNING] Making HTTP requests without a timeout can cause your app to hang indefinitely if the server is slow or unresponsive. This degrades user experience and can block critical app flows. {v3}
      prefer_try_parse_for_dynamic_data: true  # [ERROR] Using parse methods (like int.parse or double.parse) on dynamic or user-provided data can throw exceptions and crash your app if the input is invalid. This exposes your app to stability and security risks, especially when handling external or untrusted data. Always use tryParse to safely handle potentially malformed input and provide a fallback for invalid values. See https://dart.dev/guides/libraries/library-tour#numbers. {v4}
      prefer_utc_for_storage: true  # [WARNING] Local time stored without UTC conversion causes incorrect values when restored in different timezones. {v4}
      provide_correct_intl_args: true  # [ERROR] Intl.message args count does not match placeholders. This will cause runtime errors. {v2}
      require_android_permission_request: true  # [ERROR] Permission-gated API called without runtime permission request. Android 6+ will deny access or crash. {v2}
      require_animation_controller_dispose: true  # [ERROR] Neglecting to dispose of an AnimationController when a widget is removed from the tree causes memory leaks and can lead to performance degradation, as the controller continues to consume resources and tick animations in the background. This can eventually crash your app or cause unexpected behavior. Always dispose of AnimationControllers to maintain optimal app performance. See https://api.flutter.dev/flutter/animation/AnimationController/dispose.html. {v2}
      require_animation_disposal: true  # [WARNING] Failing to dispose an AnimationController in the dispose() method causes it to retain resources, listeners, and animation frames, resulting in memory leaks and degraded performance. This is especially problematic in widgets that are frequently created and destroyed, such as in lists or navigation stacks, and can lead to app instability or crashes. Dispose every AnimationController to keep Flutter apps robust. {v7}
      require_animation_ticker_disposal: true  # [ERROR] If you do not stop your Ticker in dispose(), it will continue running and leak memory, causing slowdowns and error messages. This can degrade performance and lead to crashes in long-running apps. {v2}
      require_app_startup_error_handling: true  # [WARNING] runApp() is called without runZonedGuarded or FlutterError.onError. Uncaught errors during app startup will crash the application silently with no crash report sent to monitoring services. Users see a blank or frozen screen with no diagnostic information available to the development team. {v3}
      require_apple_sign_in: true  # [ERROR] Third-party login detected without Sign in with Apple. iOS apps with social login must offer Sign in with Apple. {v2}
      require_apple_signin_nonce: true  # [ERROR] Omitting a cryptographic nonce when using Apple Sign-In exposes your app to replay attacks. Attackers can intercept a valid authorization token and reuse it to impersonate the user, gaining unauthorized access to their account and sensitive data. Apple’s security documentation strongly recommends using a unique, random nonce for every authentication request to prevent these attacks and ensure user safety. {v4}
      require_auto_route_full_hierarchy: true  # [WARNING] Using push() in auto_route bypasses the route hierarchy, so parent shell routes and wrapper widgets are not applied. This causes missing bottom navigation bars, app bars, or layout scaffolds. Use navigate() instead to push the full route tree including all parent wrappers. {v1}
      require_auto_route_guard_resume: true  # [WARNING] An AutoRouteGuard.onNavigation override must call resolver.next() on every code path. Missing calls cause navigation to hang silently with no error message, leaving the user stuck on the current page. Ensure both the if and else branches call resolver.next(true) or resolver.next(false) respectively. {v1}
      require_background_message_handler: true  # [WARNING] Push notifications received when app is terminated are silently dropped without handler. {v3}
      require_baseline_text_baseline: true  # [ERROR] CrossAxisAlignment.baseline requires a textBaseline property. Omitting it causes an assertion failure at runtime. {v2}
      require_bloc_close: true  # [ERROR] If you do not close your Bloc or Cubit in the StatefulWidget dispose() method, it will leak memory, keep stream subscriptions active, and cause app slowdowns or crashes. Always close Blocs and Cubits to prevent leaks and unexpected behavior after the widget tree is rebuilt. {v3}
      require_bloc_manual_dispose: true  # [WARNING] Bloc or Cubit holds StreamController or Timer fields but does not override close() to dispose them. Undisposed resources cause memory leaks that accumulate across navigation, eventually increasing memory pressure until the operating system kills the app. {v5}
      require_cache_key_determinism: true  # [ERROR] Cache key uses non-deterministic values (e.g., DateTime.now, Random, hashCode, or UUID). This causes cache misses, duplicated resources, and unpredictable behavior. Cache keys must uniquely and consistently identify the same resource for the same input. Using unstable values breaks cache integrity and wastes memory. {v4}
      require_cached_image_dimensions: true  # [WARNING] CachedNetworkImage without cache dimensions loads full-resolution images into memory, causing out-of-memory errors and app crashes on devices with limited RAM. Large images (such as high-resolution photos from modern cameras) can consume hundreds of megabytes when decoded, quickly exhausting available memory. {v4}
      require_camera_dispose: true  # [ERROR] Undisposed camera holds hardware exclusively, blocking other apps from accessing camera until app restart. {v2}
      require_camera_permission_check: true  # [ERROR] Camera initialized without permission check. This crashes on iOS and throws on Android. {v3}
      require_catch_logging: true  # [WARNING] Catch block swallows the exception without logging or rethrowing. Security-relevant errors such as authentication failures, permission violations, and data corruption go completely undetected. This prevents incident response, makes debugging production issues impossible, and hides potential attack activity. {v2}
      require_connectivity_timeout: true  # [WARNING] Network request has no timeout. Requests can hang indefinitely; set a timeout so they fail gracefully.
      require_data_encryption: true  # [WARNING] Unencrypted sensitive data exposes credentials to attackers via device access or backup extraction. {v5}
      require_database_migration: true  # [WARNING] Database models must support versioned migrations to handle schema changes safely. Without migration logic, updates to the schema can break data, cause runtime errors, and result in data loss or corruption. This is a critical reliability and maintainability issue for any persistent storage solution. {v4}
      require_date_format_specification: true  # [WARNING] DateTime.parse may fail on server dates due to format mismatches. Unspecified formats can cause runtime errors, silent failures, and broken data flows. This is a common source of bugs in internationalized and backend-driven apps. {v2}
      require_deep_link_validation: true  # [WARNING] Deep link parameter used without validation allows attackers to craft malicious URLs that inject arbitrary data into your app. Unvalidated deep link parameters can cause crashes from unexpected types, unauthorized access to restricted screens, or execution of unintended actions on behalf of the user. {v3}
      require_default_config: true  # [WARNING] Accessing configuration values without providing a default or fallback can cause runtime crashes if the value is missing or misconfigured. This leads to unpredictable app behavior, poor user experience, and failed startup. It may also result in app store rejection for reliability issues. {v4}
      require_dio_error_handling: true  # [WARNING] Making a Dio HTTP request without wrapping it in a try-catch block means any DioException (such as network errors, timeouts, or invalid responses) will crash the app and leave users with no feedback. This is especially problematic in production apps where network conditions are unpredictable. Always handle Dio errors to provide a robust and user-friendly experience. See https://pub.dev/packages/dio#handling-errors. {v3}
      require_dio_timeout: true  # [WARNING] Creating a Dio instance for network requests without setting connectTimeout and receiveTimeout in BaseOptions can cause HTTP requests to hang indefinitely if the server is slow or unresponsive. This can freeze the UI, degrade user experience, and make error recovery impossible, especially in mobile apps with unreliable networks. Always configure timeouts to ensure your app remains responsive and can handle network failures gracefully. See https://pub.dev/packages/dio#timeouts. {v3}
      require_error_handling_graceful: true  # [WARNING] Raw exception message or toString() is shown in UI. Show user-friendly messages instead of technical errors to improve UX and avoid leaking internal details. {v1}
      require_error_handling_in_async: true  # [WARNING] Async provider without error handling allows exceptions to propagate unhandled. Unhandled errors in FutureProvider or StreamProvider surface as uncaught exceptions that crash the app or leave the UI in a permanent loading state with no recovery path. {v3}
      require_error_identification: true  # [WARNING] Error or alert states are indicated only by color, which is not accessible to users with color vision deficiencies. All error indicators must include a non-color cue such as an icon, text label, or pattern for full accessibility. {v2}
      require_error_state: true  # [ERROR] If your BLoC state hierarchy does not include an error state, failures will be unhandled, leading to crashes or missing error UI. This makes your app less robust and harder to debug. {v3}
      require_error_widget: true  # [WARNING] FutureBuilder/StreamBuilder must handle error state to prevent silent failures, blank screens, or cryptic UI. Without error handling, users may see no feedback when data fails to load, leading to confusion, poor UX, and support burden. This can also mask backend or network issues during development. {v4}
      require_exhaustive_sealed_switch: true  # [WARNING] Switch on a sealed class uses a default or wildcard case, suppressing exhaustiveness checking. Add explicit cases for every sealed subtype so the compiler reports an error when new subtypes are introduced. {v1}
      require_fcm_token_refresh_handler: true  # [WARNING] FCM tokens expire periodically. Without onTokenRefresh handling, push notifications will stop working. {v3}
      require_feature_flag_default: true  # [WARNING] Missing default causes null/zero when remote config fetch fails, breaking expected feature behavior. {v3}
      require_field_dispose: true  # [WARNING] Disposable field may not be properly disposed. {v3}
      require_file_close_in_finally: true  # [WARNING] Unclosed file handle on exception leaks file descriptor, exhausting system limits. {v4}
      require_file_path_sanitization: true  # [WARNING] File path constructed from parameter without sanitization. Path traversal attack possible with ../ {v3}
      require_firebase_composite_index: true  # [ERROR] Firebase Realtime Database query uses orderByChild with filtering but may lack a .indexOn rule. Without the index the SDK downloads all data and sorts client-side, causing severe performance degradation on large datasets. {v1}
      require_firebase_init_before_use: true  # [WARNING] Firebase services crash if accessed before initializeApp() completes. App fails on startup. {v2}
      require_firebase_reauthentication: true  # [WARNING] Sensitive Firebase Auth operation (delete, updateEmail, updatePassword) used without reauthentication. Call reauthenticateWithCredential or reauthenticateWithProvider first. {v1}
      require_firebase_token_refresh: true  # [WARNING] getIdToken() result may be stored without token refresh. Use idTokenChanges().listen() or getIdToken(true). {v1}
      require_firestore_index: true  # [ERROR] Compound Firestore query may need a composite index. Query will fail at runtime without the required index. {v2}
      require_focus_node_dispose: true  # [WARNING] FocusNode created but not disposed. Undisposed focus nodes retain listeners and focus tree references, causing memory leaks and stale focus behavior that accumulates as users navigate between screens with form inputs. {v6}
      require_form_key: true  # [WARNING] Without GlobalKey, validate() and save() calls fail because FormState cannot be accessed. {v2}
      require_form_key_in_stateful_widget: true  # [WARNING] GlobalKey<FormState> created inside the build() method is re-instantiated on every widget rebuild triggered by setState(). Each rebuild creates a new key, causing the form to lose all current field values, validation state, and user input. This produces a frustrating user experience where typed data disappears unpredictably during state changes. {v2}
      require_geolocator_permission_check: true  # [ERROR] Accessing location without checking permission will crash your app on iOS and may cause unpredictable behavior on Android. This can result in app store rejection and poor user experience. {v2}
      require_getit_registration_order: true  # [WARNING] GetIt registration uses a dependency not yet registered at this point in the setup sequence. This causes runtime errors when the service locator attempts to resolve an unregistered type, resulting in app crashes during startup or lazy initialization that are difficult to reproduce and debug in production. {v3}
      require_getx_permanent_cleanup: true  # [WARNING] Get.put(permanent: true) registers a navigation, scroll, or animation controller that will never be automatically deleted. This can cause memory leaks if you do not manually clean up. Instances registered as permanent remain in memory for the lifetime of the app unless explicitly deleted. This is especially risky for feature-specific or page-scoped controllers that may not always be needed. {v3}
      require_getx_worker_dispose: true  # [WARNING] GetX Worker field has no corresponding dispose() call in onClose(). Without disposing the Worker, its internal stream subscription remains active after the GetxController is destroyed. This creates a memory leak where the Worker, its closure, and all captured references are retained in memory indefinitely. {v2}
      require_hive_adapter_registration_order: true  # [ERROR] Opening box before registering adapters throws HiveError. Adapters must be registered first. {v2}
      require_hive_encryption_key_secure: true  # [ERROR] Hardcoded key defeats encryption. Anyone decompiling the app can decrypt all stored user data. {v2}
      require_hive_initialization: true  # [INFO] Hive.openBox() or Hive.openLazyBox() was called without a visible Hive.init() or Hive.initFlutter() call in this file. Opening a box before initialization throws a HiveError at runtime. {v4}
      require_hive_nested_object_adapter: true  # [ERROR] Nested custom type without adapter causes runtime crash when Hive tries to serialize the object. {v2}
      require_hive_type_adapter: true  # [WARNING] Hive cannot serialize this object without @HiveType annotation. Storing will throw a HiveError at runtime. {v2}
      require_hive_web_subdirectory: true  # [ERROR] Hive.initFlutter() called without a subDir parameter. On web platforms, Hive stores data in IndexedDB. Without an explicit subdirectory, multiple apps deployed on the same domain will share the same Hive storage namespace, causing data conflicts, overwrites, and potential data loss between applications. This is especially critical for web deployments. {v2}
      require_https_for_ios: true  # [WARNING] HTTP URL will be blocked by iOS App Transport Security unless exception is configured. {v2}
      require_https_only: true  # [ERROR] HTTP URL detected in network request. Unencrypted HTTP traffic is vulnerable to interception, modification, and eavesdropping by any attacker on the network path. Sensitive data including credentials, personal information, and session tokens transmitted over HTTP can be silently captured in plaintext. {v4}
      require_image_cache_dimensions: true  # [WARNING] Image.network loads the full-resolution image into memory without cacheWidth or cacheHeight constraints. A 4000x3000 pixel photo decodes to approximately 48MB of uncompressed bitmap data in memory. Without cache dimensions, displaying multiple images causes excessive memory consumption that leads to out-of-memory crashes on lower-end devices. {v6}
      require_image_stream_dispose: true  # [WARNING] ImageStream listener is not removed in the dispose() method. The listener retains a reference to the widget State object, preventing garbage collection after the widget is removed from the tree. This creates a memory leak where decoded image data and the entire widget state remain allocated indefinitely, consuming device memory. {v2}
      require_init_state_idempotent: true  # [WARNING] initState registers a listener without a matching removeListener/removeObserver in dispose. Widget remount or hot reload can cause duplicate listeners.
      require_initial_state: true  # [ERROR] If a BLoC or Cubit does not provide an initial state, it will throw a LateInitializationError at runtime when BlocBuilder or BlocConsumer tries to read the state. This causes your app to crash and makes debugging difficult. {v3}
      require_input_validation: true  # [WARNING] Raw controller text used in network call without validation. Validate (trim, isEmpty, length, format) before sending.
      require_intl_args_match: true  # [ERROR] The placeholders in your Intl.message string do not match the arguments provided in the args list. Missing, extra, or mismatched arguments cause runtime errors, broken translations, and can result in untranslated or incorrect messages in production. This is a common source of i18n bugs and can break localization workflows. {v3}
      require_intl_plural_rules: true  # [WARNING] Manual pluralization logic using if/else or ternary expressions on count values. Languages such as Russian, Arabic, and Polish have complex plural categories (zero, one, two, few, many, other) that simple singular/plural branching cannot handle. This produces grammatically incorrect text for international users and breaks translation workflows. {v2}
      require_ios_app_tracking_transparency: true  # [ERROR] Advertising/tracking SDK detected. iOS 14.5+ requires App Tracking Transparency permission before tracking users. {v2}
      require_ios_data_protection: true  # [WARNING] Sensitive file storage detected. Consider using iOS Data Protection to encrypt files when device is locked. {v2}
      require_ios_face_id_usage_description: true  # [WARNING] Biometric authentication detected. iOS requires NSFaceIDUsageDescription in Info.plist for Face ID. {v4}
      require_ios_healthkit_authorization: true  # [WARNING] HealthKit data access detected. Ensure authorization is requested before reading or writing health data. {v2}
      require_ios_keychain_for_credentials: true  # [ERROR] Credential storage in SharedPreferences detected. Use iOS Keychain (flutter_secure_storage) for sensitive data. {v2}
      require_ios_local_notification_permission: true  # [WARNING] Local notification scheduling detected. Ensure iOS notification permission is requested before scheduling. {v2}
      require_ios_permission_description: true  # [WARNING] Missing Info.plist keys cause App Store rejection or instant crash when the permission is requested. {v5}
      require_ios_privacy_manifest: true  # [WARNING] API requires an iOS Privacy Manifest entry (iOS 17+). Missing PrivacyInfo.xcprivacy declarations for required-reason APIs will cause automatic App Store rejection during review, block new releases, and may trigger runtime permission failures on user devices. {v3}
      require_ios_receipt_validation: true  # [WARNING] In-app purchase detected. Ensure receipt is validated with server, not just locally. {v2}
      require_key_for_reorderable: true  # [ERROR] Without unique keys, reordering fails silently or shows wrong items after drag-and-drop. {v3}
      require_late_initialization_in_init_state: true  # [WARNING] Late field initialized in build() recreates the object on every setState() call. AnimationController instances restart their animations, StreamSubscription objects create duplicate listeners, and the resulting UI jank becomes visible to users. This also wastes memory by allocating new objects on every widget rebuild cycle. {v3}
      require_lifecycle_observer: true  # [WARNING] Long-running timers, streams, or background tasks must be paused or stopped when the app is backgrounded. Failing to observe lifecycle changes can drain battery, consume CPU, and cause stale callbacks to execute when the app resumes, leading to inconsistent state and poor user experience. This may also violate platform guidelines and result in app store rejection. {v4}
      require_location_permission_rationale: true  # [WARNING] Location permission requested without showing rationale. Users may deny without understanding why. {v2}
      require_map_idle_callback: true  # [INFO] Data fetching triggered in onCameraMove fires on every frame during map pan and zoom gestures. This spams backend APIs with hundreds of redundant requests per second, causes severe performance degradation with UI jank, and wastes user bandwidth and battery on mobile devices. {v3}
      require_media_loading_state: true  # [INFO] VideoPlayer displayed without checking isInitialized shows a black rectangle or crashes. The video player needs time to load the video before the build method can render it properly. {v4}
      require_media_player_dispose: true  # [ERROR] Undisposed media controller holds audio/video hardware, blocking other apps and draining battery. This can cause resource leaks, app crashes, and poor user experience. {v2}
      require_minimum_contrast: true  # [WARNING] Low contrast text excludes users with low vision, cataracts, or age-related sight loss who cannot distinguish foreground from background. WCAG 2.1 Success Criterion 1.4.3 requires a minimum contrast ratio of 4.5:1 for normal text and 3:1 for large text. Failing to meet this threshold makes content unreadable for millions of users worldwide. {v2}
      require_mock_http_client: true  # [WARNING] Real HTTP calls in tests create network dependencies that cause flaky failures and slow CI pipelines. Tests become non-deterministic, failing on timeout when servers are slow and passing inconsistently across environments, making test results unreliable for merge decisions. {v5}
      require_mounted_check: true  # [ERROR] Calling setState after an await without checking if the StatefulWidget is still mounted in the widget tree throws a "setState called after dispose" error. This leads to runtime exceptions, app instability, and hard-to-debug crashes, especially in async code. {v4}
      require_mounted_check_after_await: true  # [ERROR] setState called after await without mounted check. Widget may have been disposed. {v2}
      require_native_resource_cleanup: true  # [WARNING] Unfreed native memory leaks outside Dart GC, causing permanent memory loss until app restart. {v3}
      require_notification_channel_android: true  # [WARNING] Android notification is missing a channel ID or description. Without these, notifications may not appear or may be grouped incorrectly on Android 8.0+ devices, reducing reliability and user engagement. {v2}
      require_page_controller_dispose: true  # [ERROR] Not disposing of a PageController when a widget is removed from the tree can cause memory leaks and continued resource usage, leading to performance issues and potential crashes in long-running apps. This is especially critical in apps with dynamic navigation or frequent page changes. See https://api.flutter.dev/flutter/widgets/PageController/dispose.html. {v4}
      require_pagination_for_large_lists: true  # [WARNING] ListView/GridView builder uses itemCount from a list that may be bulk-loaded, risking OOM and slow UI. Use PagedListView (infinite_scroll_pagination) or cursor-based pagination to load items in pages. {v1}
      require_provider_dispose: true  # [WARNING] Provider creating a ChangeNotifier without a dispose callback leaks listener registrations and memory. Over time, leaked notifiers accumulate stale listeners that fire after the StatefulWidget is removed from the tree, causing setState-after-dispose errors and increasing memory pressure. {v4}
      require_provider_scope: true  # [ERROR] If your Riverpod app is missing ProviderScope at the root, any attempt to access providers will throw runtime exceptions and crash the app. This makes the app unusable and breaks all provider-based state management. {v2}
      require_purchase_restoration: true  # [ERROR] In-app purchase detected without restore functionality. App Store requires "Restore Purchases" for non-consumables. {v2}
      require_purchase_verification: true  # [ERROR] In-app purchase without server verification detected. Client-side verification can be bypassed by attackers. {v2}
      require_qr_content_validation: true  # [WARNING] QR code scan result used without validation creates a security vulnerability. Malicious QR codes can contain crafted URLs that redirect to phishing sites, inject JavaScript through URL schemes, trigger automatic downloads, or launch unintended app actions. Users scanning codes in public places are especially vulnerable. {v2}
      require_qr_permission_check: true  # [ERROR] If you open the QR scanner without first requesting camera permission, your app will show a black screen on iOS or crash on some Android devices. This breaks user experience and can cause app store rejection. {v2}
      require_riverpod_error_handling: true  # [WARNING] AsyncValue from a Riverpod provider accessed without handling error and loading states. When the async operation fails or is in progress, accessing .value directly throws a StateError or returns null, causing crashes or displaying stale data. Users see broken UI instead of loading indicators or error messages. {v2}
      require_route_guards: true  # [ERROR] Protected route path (profile, settings, admin, payment) lacks an authentication guard. Without a redirect callback, unauthorized users can access sensitive pages directly via deep link or URL manipulation, exposing personal data, financial information, or admin controls to unauthenticated sessions. {v3}
      require_scroll_controller_dispose: true  # [ERROR] ScrollController created but not disposed. Undisposed scroll controllers retain listeners and scroll position state, causing memory leaks that accumulate as users navigate between screens with scrollable content. {v5}
      require_search_debounce: true  # [WARNING] Calling a search API on every keystroke can overwhelm your backend, degrade performance, and create a poor user experience. Without debouncing, users may see lag, rate limits, or unnecessary network traffic. {v3}
      require_secure_key_generation: true  # [ERROR] Encryption key generated from a predictable source. Key.fromLength() uses dart:math Random which is NOT cryptographically secure. Hardcoded byte arrays in Uint8List.fromList() or List.filled() produce deterministic keys extractable from compiled binaries. Predictable keys compromise all data encrypted with them and cannot be rotated without data loss. {v2}
      require_secure_password_field: true  # [WARNING] Password input fields must disable suggestions and autocorrect to prevent sensitive information from being stored in device dictionaries or suggested to users inappropriately. Failing to do so increases the risk of password leakage, accidental exposure, and poor user privacy. Always configure password fields to maximize security and user trust. {v5}
      require_secure_storage_for_auth: true  # [ERROR] Auth tokens in SharedPreferences leak via backup extraction, enabling account takeover. {v4}
      require_speech_stop_on_dispose: true  # [ERROR] Unreleased SpeechToText keeps microphone active, draining battery and blocking other apps. {v2}
      require_sqflite_whereargs: true  # [ERROR] SQL queries using string interpolation are vulnerable to SQL injection attacks. User input can modify queries, leak data, or corrupt the database. Always use whereArgs to safely pass parameters. {v3}
      require_sse_subscription_cancel: true  # [ERROR] If a widget (such as a StatefulWidget managing a live data feed) opens a Server-Sent Events (SSE) or EventSource connection but does not cancel it in dispose(), the connection remains open after the widget is removed. This can result in orphaned network connections, wasted bandwidth, and memory leaks, especially in dashboards, chat clients, or any UI with dynamic SSE usage. Always close SSE/EventSource connections in the correct State object’s dispose() method. See https://docs.flutter.dev/perf/memory#dispose-resources. {v4}
      require_stream_controller_close: true  # [ERROR] Failing to close a StreamController in the dispose() method leaves the stream open, causing memory leaks, connection handle exhaustion, and potential app crashes. Unclosed streams can accumulate events and listeners, degrading performance and making debugging difficult. Proper closure is essential for robust, production-quality Flutter apps. {v5}
      require_stream_controller_dispose: true  # [ERROR] Not closing a StreamController in the StatefulWidget dispose method will leak memory, keep listeners active, and cause app slowdowns, crashes, or persistent background activity. This leads to stream subscription exhaustion, battery drain, and unpredictable bugs, especially in production apps with frequent widget tree rebuilds. Unclosed controllers also prevent garbage collection and block app updates. {v7}
      require_stream_subscription_cancel: true  # [WARNING] StreamSubscription field without cancel(). Callbacks fire after State disposal, causing setState errors and memory leaks. {v4}
      require_subscription_status_check: true  # [WARNING] Premium/subscription content displayed without status verification. Subscription may have expired. {v3}
      require_supabase_realtime_unsubscribe: true  # [ERROR] Unsubscribed channel keeps WebSocket open, leaking connections and receiving stale updates. {v2}
      require_tab_controller_dispose: true  # [ERROR] Undisposed TabController leaks AnimationController, causing memory exhaustion over time. {v2}
      require_tab_controller_length_sync: true  # [ERROR] TabController length mismatch throws RangeError when switching tabs, crashing the app. {v2}
      require_test_widget_pump: true  # [ERROR] After simulating a tap or text entry in a widget test, failing to call pump() or pumpAndSettle() means the widget tree will not rebuild, so your test may pass even though the user experience is broken. Always process pending build events to ensure your test reflects real app behavior. {v3}
      require_text_editing_controller_dispose: true  # [ERROR] When a State class creates a TextEditingController for a TextField, search bar, or form input and fails to dispose of it in dispose(), the controller’s internal listeners and text buffers will persist after the widget is removed from the widget tree. This can cause memory leaks, input lag, and unpredictable or stale text field behavior, especially in dynamic forms, chat UIs, or lists where widgets are frequently created and destroyed. In severe cases, this can lead to app slowdowns or crashes due to uncollected resources. Always dispose of every TextEditingController in the owning State object’s dispose() method to ensure proper cleanup. See: https://api.flutter.dev/flutter/widgets/TextEditingController-class.html {v3}
      require_type_adapter_registration: true  # [WARNING] Hive box opened with a custom type but the corresponding TypeAdapter may not be registered. Unregistered adapters cause HiveError at runtime, preventing all read and write operations on the box, which leads to app crashes and complete data inaccessibility. {v3}
      require_unique_iv_per_encryption: true  # [ERROR] Using a static or reused initialization vector (IV) with the same encryption key enables attackers to detect patterns in your encrypted data. When the same IV is used for multiple encryption operations, identical plaintexts will always produce identical ciphertexts, making it possible for adversaries to infer relationships between messages, perform replay attacks, or even recover plaintexts in some encryption modes. This breaks the fundamental guarantee of confidentiality provided by encryption and has led to serious vulnerabilities in real-world systems. {v4}
      require_unknown_route_handler: true  # [ERROR] MaterialApp or CupertinoApp defines routes or onGenerateRoute but does not provide an onUnknownRoute handler. When a user navigates to an undefined route path via deep link, push notification, or programmatic navigation, the app throws an unhandled exception and crashes instead of showing a helpful error screen. {v4}
      require_url_validation: true  # [WARNING] Uri.parse on user input without scheme validation enables SSRF attacks. Attackers can make your app connect to internal servers, databases, or use malicious protocols to exfiltrate data. {v5}
      require_value_notifier_dispose: true  # [ERROR] If you do not dispose a ValueNotifier, it will leak memory, keep listeners attached, and trigger updates on a StatefulWidget that has already been removed from the widget tree. This causes memory leaks, unexpected UI updates, and hard-to-find bugs in your app. {v6}
      require_vsync_mixin: true  # [ERROR] AnimationController is missing a vsync parameter. Without vsync, animations run without frame synchronization, causing visual tearing, wasted CPU cycles, and degraded user experience. This can lead to janky motion and battery drain, especially on mobile devices. Always provide vsync: this and mix in SingleTickerProviderStateMixin to ensure smooth, efficient animations and proper animation controller lifecycle management. {v4}
      require_websocket_close: true  # [WARNING] Unclosed WebSocket leaks connections and continues receiving data after widget disposal, causing errors. {v5}
      require_websocket_message_validation: true  # [WARNING] Unvalidated WebSocket messages crash when malformed data arrives, or enable injection attacks. {v3}
      require_websocket_reconnection: true  # [WARNING] WebSocket connection without reconnection logic will stay permanently disconnected after network interruptions, server restarts, or mobile network handoffs. Users will see stale data, miss real-time updates, and have no indication that the live connection has dropped until they manually refresh or restart the app. {v4}
      require_webview_ssl_error_handling: true  # [ERROR] If your WebView does not handle SSL certificate errors, it may silently accept invalid or malicious certificates, exposing users to man-in-the-middle attacks. Users may unknowingly submit sensitive information (such as passwords or payment details) to attackers, resulting in account compromise, data theft, or financial loss. Proper SSL error handling is essential for secure in-app browsing. {v4}
      require_workmanager_result_return: true  # [ERROR] Missing return value makes WorkManager assume failure, triggering unnecessary retries. {v2}
      use_ref_and_state_synchronously: true  # [WARNING] Using ref after await risks accessing disposed provider, causing runtime errors or stale data. {v2}
      use_ref_read_synchronously: true  # [WARNING] ref.read() after await may access a disposed or invalidated provider, causing crashes or stale reads. {v2}

