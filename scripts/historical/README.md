# Scripts history

One-off or superseded scripts kept for reference. Not part of the normal publish or audit workflow.

- **add_rule_instantiation_batch.py** — Used once to add the Rule Instantiation test group to category test files. All categories now have that group; script retained in case new categories are added later.
- **check_roadmap_implemented.py** — Superseded by `scripts/modules/_roadmap_implemented.py`. Roadmap cleanup now runs as an auto-fix in Step 1 of the publish script.
